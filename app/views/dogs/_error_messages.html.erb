<%= turbo_frame_tag 'dog_image' do %>
  <% if errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(errors.count, 'error') %> prohibited this @dog from being saved:</h2>

      <ul>
        <% errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<% end %>
